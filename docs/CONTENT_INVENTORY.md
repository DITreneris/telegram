# Content inventory (posts, PNG, quizzes)

Auto-generated by `python scripts/audit_posts_png_quizzes.py --write-inventory docs/CONTENT_INVENTORY.md`.
Re-run after changing `posts.json`, `web/public/images/posts/`, or `data/polls.json`.

## Summary

| Metric | Value |
|--------|-------|
| posts.json rows (A) | 74 |
| PNG on `web/public/images/posts/` (D) | 74 |
| Distinct image basenames in JSON (C) | 74 |
| Poll items in `data/polls.json` | 74 |
| Post ids with no PNG file (orphan rows vs disk) | 0 |
| PNG slots with no post row (before merge) | 0 |

## Principle

Target: one row per `NN_Prompt_Anatomy.png` on disk, with `id == NN` and `image` = `/images/posts/NN_Prompt_Anatomy.png`. Run `python scripts/merge_posts_json_from_png.py --write` to align.

## Quiz coverage

- Posts without any linked poll: **0** (see audit stdout for ids).
- Full 1:1 quiz coverage is optional; add rows to `data/polls.json` when content is ready.

## Raw audit

```
=== Canonical inventory (same labels as audit_post_images.py) ===
  A  posts.json rows:                74
  B  distinct post id:             74
  C  distinct image basenames:       74
  D  PNG files web/.../posts:        74
  E  PNG files data/images:          74
  F  posts with image on WEB path:   74  (of 74)
  G  JSON refs missing on web:       0
  H  JSON refs only in data:         0
  I  WEB PNGs not referenced by JSON: 0

=== 1:1 principle (id == slot from NN_Prompt_Anatomy.png) ===
  (no id/slot/filename mismatches for valid basenames)
  (no multi-post reuse of same basename)

=== PNG set vs post id set ===
  Slots with a PNG file (count):     74
  Post rows whose id has no PNG:     0  []
  PNG files with no post id row:     0  []

=== polls.json (quiz bank) ===
  Poll items:                        74
  Duplicate related_post_id:         none
  Post ids with at least one poll:   74
  Post ids without any poll:         0

Commands: python scripts/audit_post_images.py
          python scripts/gen_post_images_gap_report.py
          python scripts/merge_posts_json_from_png.py --dry-run
```
